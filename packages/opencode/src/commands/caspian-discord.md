---
description: Post a Discord message via Caspian (channel snowflake id)
---

Load the **caspian-discord** skill (alias caspian:Discord), then call tool
`caspian_send_discord` with `to` (channel snowflake id) and `body`.

Always note: invite the bot first; `to` is Copy Channel ID (Developer Mode).

Do not claim success without a tool result.

Arguments:
$ARGUMENTS
